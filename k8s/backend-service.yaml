apiVersion: v1
kind: Service
metadata:
  name: canary-backend
  namespace: notes
spec:
  selector:
    app: canary-backend
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
  type: NodePort
