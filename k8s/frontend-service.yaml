apiVersion: v1
kind: Service
metadata:
  name: canary-frontend
  namespace: notes
spec:
  selector:
    app: canary-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: NodePort
